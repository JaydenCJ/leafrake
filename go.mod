// leafrake — classifies local git branches (merged, squash-merged, stale,
// gone) and deletes the dead ones with per-branch evidence.
//
// version:    0.1.0
// author:     JaydenCJ
// license:    MIT
// repository: https://github.com/JaydenCJ/leafrake
// keywords:   git, branches, cleanup, squash-merge, prune, cli, developer-tools
//
// Zero runtime dependencies: standard library only.
module github.com/JaydenCJ/leafrake

go 1.22
